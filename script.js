const mic=document.getElementById('mic'),st=document.getElementById('status'),ls=document.getElementById('listen');const SR=window.SpeechRecognition||window.webkitSpeechRecognition;if(SR){const r=new SR();r.lang='ko-KR';r.interimResults=true;mic.onclick=()=>{mic.classList.add('hide');ls.classList.remove('hidden');st.textContent='';r.start();};r.onresult=e=>{let t='';for(let i=0;i<e.results.length;i++)t+=e.results[i][0].transcript;st.textContent=t;};r.onend=()=>{ls.classList.add('hidden');st.classList.add('done');setTimeout(()=>st.classList.remove('done'),600);setTimeout(()=>mic.classList.remove('hide'),1200);};}else{st.textContent='음성인식을 지원하지 않습니다.';}